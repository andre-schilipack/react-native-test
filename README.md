# react-native-test
Project for react native tests


To start:

- clone
- bundle install
- rake db:create
- rake db:migrate
- gem install mailcatcher(will take a while, can't put on gemfile)
- mailcatcher
- rails s
